name: psd_sdk
description: A Dart library that directly reads Photoshop PSD files. Supports limited export functionality.
version: 0.2.1
repository: https://github.com/nexo-tech/psd_sdk

environment:
  sdk: '>=2.17.0'

dependencies:
  archive: ^4.0.7

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.2
