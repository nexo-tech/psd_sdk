name: psd_sdk
description: A Dart library that directly reads Photoshop PSD files. Supports limited export functionality.
version: 0.2.0
homepage: https://github.com/nexo-tech/psd_sdk

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  archive: ^2.0.13

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
